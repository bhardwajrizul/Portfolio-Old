{
  "name": "portfolio",
  "version": "1.0.0",
  "description": "Rizul's Portfolio Webiste",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start-parcel": "parcel index.html",
    "build": "parcel build index.html",
    "clean": "rm -rf .parcel-cache dist",
    "watch-css": "npx tailwindcss -i ./css/styles.css -o ./css/output.css --watch",
    "start": "npm-run-all --parallel start-parcel"
  },
  "author": "Rizul Bhardwaj",
  "license": "ISC",
  "dependencies": {
    "gsap": "^3.12.4"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "parcel": "^2.11.0",
    "postcss": "^8.4.33"
  }
}
